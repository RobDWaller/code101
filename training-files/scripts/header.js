/**
 * Add header text to the HTML page, this just adds a line of text to the H1
 * element to the HTML page
 *
 * @author Rob Waller <rdwaller1984@googlemail.com>
 */
var header = function () {

    // Create a header variable and assign it the value you wish.
    var header = '';

    // Get the H1 header element from the HTML page
    //var headerElement = document.getElementById();

    // Set the header element's value to match the header variable
    //headerElement.innerHTML = ;
};
