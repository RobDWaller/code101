/**
 * Add title text to the HTML page, this just adds a line of text to the H1
 * element to the HTML page
 *
 * @author Rob Waller <rdwaller1984@googlemail.com>
 */
var title = function () {

    // Create a title variable and assign it the value you wish.
    var title = '';

    // Get the H1 title element from the HTML page
    //var titleElement = document.getElementById();

    // Set the title elements value to match the title variable
    //titleElement.innerHTML =;
};
